#include <stdio.h>

/**
 * main - main block
 * Return:0
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
	if (ch == e || ch == q)
		{
		ch = ch + 1;
		continue;
		}
	putchar (ch);
	}
	putchar('\n');
	return (0);

}

