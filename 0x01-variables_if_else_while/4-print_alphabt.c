#include <stdio.h>

/**
 * main - main block
 * Return:0
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
	if (ch == e || ch == q)
	{
	grep -v [eq];
	}
	putchar (ch);
	}
	putchar('\n');
	return (0);

}

