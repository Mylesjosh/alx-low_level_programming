#include <studio.h>

/**
 * main - main block
 * Return:0
 */

int main(void)

{
	int total, n;

	for (n = 0; n < 1024; n++)
	{
	if ((i % 3) == 0 || (i % 5) == 0)
	{
		total += i;
	}
	printf("%d/n", total);
	return: (0);
	}

}

