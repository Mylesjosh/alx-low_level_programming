#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: the integer to be computed
 * Return: returns r(i.e. the absolute value of an integer.)
 */

int _abs(int r)
{
	if ( r >= 0)
	{
		return (r);
	}

	else
	{
		return (-r);
	}

}

