#include <stdio.h>

/**
 * main - main block
 * @argc: argument count
 * @argv: argument vector
 * Return:0
 */

int main(int argc, char *argv[])
{
	printf("%s"argv[0]);

	ignore(argc);

	return (0);
}
